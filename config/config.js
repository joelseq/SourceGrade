// Copy this file as config.js in the same folder, with the proper database connection URI.

module.exports = {
  db: 'mongodb://localhost:27017/mern-boilerplate',
  db_dev: 'mongodb://url:port/db',
};
